[project]
name = "life-coach"
version = "0.1.0"
description = "격려하는 Life Coach Agent — OpenAI Agents SDK + Streamlit + Web Search"
requires-python = ">=3.13"
dependencies = [
    "openai-agents>=0.2.6",
    "python-dotenv>=1.1.1",
    "streamlit>=1.48.0",
]
