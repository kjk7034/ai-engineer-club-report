[project]
name = "movie-expert-agent"
version = "0.1.0"
description = "Movie Expert Agent with OpenAI tool calling"
requires-python = ">=3.14"
dependencies = [
    "openai>=2.41.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
    "python-dotenv>=1.2.2",
]
